/**
 * Public launch controls for independently releasable feature levels. Premium
 * capabilities remain fail closed until an intentional code change exposes
 * them in a production build.
 */
export const BASIC_READ_ALOUD_PUBLICLY_AVAILABLE = true;
export const PREMIUM_READ_ALOUD_PUBLICLY_AVAILABLE = false;
